---
name: workbuddy-checkin
description: 使用当前 WorkBuddy 或 CodeBuddy CN 账号签到（含本次获得/累计获得/真实余额三字段）。用户要求签到、打卡、check in 时使用。
---

# WorkBuddy 签到

用户明确要求签到后，直接运行，不要先读脚本：

```sh
python3 <skill-directory>/scripts/workbuddy-checkin.py
```

脚本会直接输出格式化的中文签到结果（含本次获得 / 累计获得 / 真实余额）。只回复一次：将脚本输出的中文结果原样返回给用户。不要泄露凭据或重试。不要添加任何额外解释。

## 自动签到

用户说"每天定时签到 / 设置自动签到 / 帮我每天打卡"时，直接创建一个 WorkBuddy 自动化（不要展示命令，不要在对话里让用户手写任何内容）：

- **名称**：`WorkBuddy 每日签到`
- **执行频率**：用户指定的时间；未指定则默认每天 `09:00`
- **执行命令**：

  ```sh
  python3 <skill-directory>/scripts/workbuddy-checkin.py
  ```

- **自动化 Prompt**（原样填入，不要改写）：

  ```
  执行签到任务。运行以下命令：

  <skill-directory>/scripts/workbuddy-checkin.py

  然后只做一件事：把脚本在终端输出的内容，从以「✅」开头的那一行开始、到「---」分隔符之前结束，原样作为企业微信消息发出（平台会自动在开头显示「自动化任务【WorkBuddy 每日签到】」作为头部，无需你再加）。

  硬性要求（违反任意一条都不行）：
  - 你的回复只能包含上述脚本结果，一个字都不能多。
  - 禁止输出任何英文、任何思考过程、任何状态说明、任何"执行成功"之类的话。
  - 禁止读取或写入任何记忆文件（memory），禁止引用之前的对话历史，禁止重复聊天里出现过的文字。
  - 不要自己添加标题行（头部由平台提供）。
  - 若脚本没有输出「✅」结果行（即报错），则只发送报错信息本身。
  ```

企业微信推送开关需用户在自动化设置里手动开启一次（脚本不含通知功能）。修改时间或开关请直接在 WorkBuddy 自动化设置里操作，本技能脚本不参与定时调度。
